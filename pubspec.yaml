name: shared_preferences_package_flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Shared Preferences
  shared_preferences: ^2.2.2
  #Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  # Icons
  icons_plus: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/